# v3 — Scale + Intelligence (PLAN, stub)

> **Status: stub.** v3 is not open yet. This file is a placeholder so the docs
> tree and the active-phase pointer have somewhere to land. Flesh it out at the
> v3 kickoff (a `ce-plan` / brainstorm pass), then cut the `v3` phase branch off
> `main` per [`docs/WORKFLOW.md`](../../WORKFLOW.md).

## Inputs
- Carried-over follow-ups + themes: [`docs/brainstorm/v3-backlog.md`](../../brainstorm/v3-backlog.md).
- Prior phase record: [`docs/archive/v2/`](../../archive/v2/).

## Theme (direction only)
Scale beyond Lollapalooza to many festivals, with search and recommendations that
hold up at that scale, and a real user-data backend (so favorites/playlists sync
across devices instead of living only in device-local IndexedDB).

## Segments — TBD at kickoff
Segment `v3.0` will be foundation/restructure as usual. Everything else is open.
